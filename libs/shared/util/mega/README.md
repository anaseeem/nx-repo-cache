# shared-util-mega

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-util-mega` to build the library.

## Running unit tests

Run `nx test shared-util-mega` to execute the unit tests via [Jest](https://jestjs.io).
