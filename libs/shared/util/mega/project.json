{
  "name": "shared-util-mega",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/util/mega/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project shared-util-mega --web",
  "targets": {
    "build": {
      "executor": "@nx/rollup:rollup",
      "options": {
        "cwd": "{projectRoot}",
        "tsConfig": "{projectRoot}/tsconfig.lib.json",
        "main": "{projectRoot}/src/index.ts",
        "outputPath": "{workspaceRoot}/dist/{projectRoot}",
        "assets": ["{projectRoot}/*.md", "{projectRoot}/src/**/!(*.ts)"],
        "clean": false,
        "additionalEntryPoints": ["{projectRoot}/src/foo.ts"],
        "generateExportsField": true
      },
      "outputs": ["{options.outputPath}"]
    }
  }
}
