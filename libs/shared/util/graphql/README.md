# shared-util-graphql

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-util-graphql` to build the library.

## Running unit tests

Run `nx test shared-util-graphql` to execute the unit tests via [Jest](https://jestjs.io).
